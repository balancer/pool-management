import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div``;

const RemoveAssetsTable = () => {
    return <Wrapper></Wrapper>;
};

export default RemoveAssetsTable;
